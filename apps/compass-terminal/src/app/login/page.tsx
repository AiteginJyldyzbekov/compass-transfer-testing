import { LoginPage } from '@pages/terminal/login';

export default function Page() {
  return <LoginPage />;
}
export const metadata = {
  title: 'Вход в систему | Compass 2.0',
  description: 'Страница входа в административную панель Compass 2.0',
};
